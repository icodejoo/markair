# YAML 样例:多行标量与锚点的词法陷阱(T67 语料,含中文注释)

# 默认配置,用锚点定义,供下方引用
默认配置: &默认值
  超时: 30
  重试次数: 3

用户A:
  <<: *默认值
  名字: 张三

# 折叠标量(> ):折行会被替换为空格,常用于长句子
简介: >
  这是一段很长的中文简介,
  跨越多行,但最终会被折叠成一行,
  用来测试折叠标量的词法处理。

# 块标量(| ):保留换行,常用于多行脚本或诗句
脚本: |
  echo "你好,世界"
  echo "第二行,保留换行"
