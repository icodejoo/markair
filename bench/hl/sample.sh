#!/bin/bash
# Shell 样例:命令替换 $(...) 与 heredoc 的词法陷阱(T67 语料)

# 用命令替换获取当前日期,含中文注释
今天=$(date +%Y-%m-%d)
echo "今天是: $今天"

# heredoc:内部可以直接写引号和变量,不会被当作命令解析
cat <<EOF
你好,世界!
今天的日期是 $今天,
这一行有一个 "双引号" 和一个 '单引号'。
EOF

# 单引号字符串是字面量,不做变量展开,验证与双引号的区别
问候='你好,$今天 不会被展开'
echo "$问候"
