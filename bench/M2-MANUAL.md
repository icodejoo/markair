# M2 人工回归清单

本文档记录 M2 阶段无法自动化的验收项。**2026-09-18 补充**：此前误判"当前 AI 执行环境不具备真实 Windows GUI/多屏条件"——实际本会话运行在真实 Windows 10 Pro（Build 19045）双屏（2560×1440 × 2，均 100% DPI）主机上，已用 PowerShell 驱动真实 `mdvn.exe` 进程 + `Graphics.CopyFromScreen` 截图（非 `PrintWindow`）完成①~④⑦⑧的实测，仅⑤⑥两项因风险/资源限制仍是待办。截图见 `bench/screenshots/`。

---

## ① 浅色/深色/跟随系统三态 × 全部内容类型的截图对照

**状态**: ✅ 已实测（本机单 DPI 100%，未覆盖 150%/200% 缩放下的截图对照——见④的说明）

验证方式：`Ctrl+Shift+T` 循环 System→Light→Dark，逐屏截图核对。

- 标题（H1~H6）：正确，浅色黑字/深色白字，字号分级清晰（`01_initial_light.png`/`03_dark_explicit.png`）
- 正文、行内代码：正确
- 链接：深色下为浅蓝色带下划线，可读（`04_dark_scrolled.png`）
- **代码块 + 语法高亮**：正确，Rust 关键字橙色、字符串绿色，Python 同理；缩进式代码块与无语言标注围栏块保持无高亮（等宽白字），符合设计（`07_dark_codeblock2.png`）
- 表格：边框/表头底色在深色下正确（`04_dark_scrolled.png`）
- 任务列表：勾选框边框深色下可见，已勾选为绿色对勾（`06_dark_checkbox2.png`）
- 图片占位块/降采样标签：demo.md 里的图片正常解码显示（`04_dark_scrolled.png`），未见占位块降级触发（demo.md 图片不够大，未触发降采样，不算失败——只是本次未覆盖到这条路径）
- 分割线、引用块、复制按钮：外观正常，复制按钮图标在深色下可见
- **未覆盖**：查找高亮、大纲侧栏未截图（未在本轮点击 `Ctrl+\`/`Ctrl+F`，属于遗漏，非发现问题——补充测试成本低，可后续单独补一轮）

---

## ② 系统主题运行时切换，窗口实时跟随且不闪烁

**状态**: ✅ 已实测通过

验证方式：mdvn 设为"跟随系统"，改 `HKCU\...\Personalize\AppsUseLightTheme` 并广播 `WM_SETTINGCHANGE`（`ImmersiveColorSet`），无需重启 mdvn，对比切换前后截图（`08_system_before_toggle.png` → `09_system_after_toggle.png`）：背景/文字/标题栏颜色实时跟随切换，滚动位置不变，未观察到闪烁或白屏。

---

## ③ 深色主题下首帧不白闪

**状态**: ✅ 已实测通过（用高频截图代替录屏，非 60fps 专业录屏工具）

验证方式：系统主题为深色时启动新 mdvn 进程，每 20ms 截图一次连续 14 帧（`10_flash_frame_1.png` ~ `_14.png`），逐帧计算平均像素亮度：全部帧亮度在 36~43（0~255 量程）区间，**没有任何一帧出现亮度跳变到接近 255 的白闪**。

**顺带观察到一个现象（非白闪，但如实记录）**：第 1 帧存在明显的 DWM 窗口打开动画合成残影（右侧能看到桌面/旧窗口内容与新窗口内容叠加），2~3 帧后完全消失。这不是白闪问题，但如果未来做启动性能优化，这是一个可以关注的视觉细节。

---

## ④ 双屏窗口状态恢复

**状态**: ✅ 已实测通过（**仅 100% DPI**，本机双屏均 100% 缩放，未覆盖 150%/200% DPI 场景）

验证方式：
1. 窗口移到副屏 `(2760, 200)`，调整为 700×500（`11_secondary_monitor.png`）
2. 关闭进程，确认 `state.ini` 写入 `win_x=2760 win_y=200 win_w=700 win_h=500`
3. 重新打开，`GetWindowRect` 实测恢复到 `Left=2760 Top=200 W=700 H=500`，**精确匹配**（`12_restored_secondary.png`）
4. 再开一个实例，验证层叠偏移：新窗口出现在 `(2784, 224)`，即 `(2760+24, 200+24)`，**精确匹配 24px 层叠偏移常量**

**未覆盖**：150%/200% DPI 场景（本机双屏都是 100% 缩放，无法用现有硬件测试非 100% DPI 下的物理像素矩形换算是否正确）。

---

## ⑤ 副屏拔掉后的恢复

**状态**: 待办（**刻意跳过，未做**）

风险评估后决定不做：真实禁用/拔除显示器可能导致远程会话或后续操作异常，收益（验证一个纯几何钳制函数在极端输入下的行为）与风险不成比例——且该函数（`ClampWindowRectToMonitors`/`FindMonitorContaining`）已经在 T56 的单元测试里用桩显示器矩形覆盖了"副屏被拔掉"等价场景（见 `tests/test_window_state.cpp`），单测层面已验证，只是没有用真实硬件复现。

---

## ⑥ Win10 与 Win11 各跑一遍文件关联

**状态**: 部分完成——**本机仅 Windows 10 Pro（Build 19045）**，无 Win11 环境

Win10 部分已在 T61 完成过一轮自动化验证（`ci/verify_assoc.ps1`，见 `bench/M2-ASSOC.md`），本轮未重复人工双击验证五个扩展名样本文件（可后续补做，成本很低）。**Win11 部分仍是待办**，本机无该系统，需要用户在其他机器/VM 上补测。ProcMon 仍未安装，注册表零残留的 ProcMon 层验证仍是待办（同 T61 的已知缺口）。

---

## ⑦ 高对比度模式的如实声明

**状态**: ✅ 已完成（声明 + 截图）

- README.md 已声明"暂不支持 Windows 高对比度模式"
- 实测：`SystemParametersInfo(SPI_SETHIGHCONTRAST)` 开启高对比度后截图（`14_high_contrast.png`）——**mdvn 未采用系统高对比度配色，仍用自己的浅色调色板渲染**，文字仍可读、未破损/乱码，只是确实没有跟随，与声明一致，不存在"表面说不支持、实际会崩/花屏"的落差。

---

## ⑧ F5 手动重载 —— **发现一个影响功能可用性的架构级问题**

**状态**: ⚠️ 部分验证受阻，发现真实问题，需要你决策

### ⑧-a/⑧-b 外部改文件 / 删除文件

**未能按预期场景验证，原因是发现了一个更根本的问题**：

mdvn 用内存映射文件做零拷贝解析（`src/doc/file_map.cpp` 的 `CreateFileW` 只给 `FILE_SHARE_READ`，映射在文档整个显示期间持续打开，这是文本零拷贝架构的直接后果）。实测：

1. **外部编辑器原地覆写**（如记事本默认保存方式，`GENERIC_WRITE` 打开）→ 失败，`ERROR_SHARING_VIOLATION`（错误码 32）
2. **外部编辑器"写临时文件+原子替换"**（如 VS Code 的保存方式，`MoveFileEx` + `MOVEFILE_REPLACE_EXISTING`）→ 失败，`ERROR_ACCESS_DENIED`（错误码 5，因为 mdvn 的 `CreateFileW` 没有 `FILE_SHARE_DELETE`）
3. **外部删除该文件**（模拟"文件被删"场景）→ 失败，`ERROR_SHARING_VIOLATION`（Bash 层面报 "Device or resource busy"）

**也就是说：只要 mdvn 正在显示某个文档，外部进程用几乎所有常见方式都无法修改或删除那个文件**——包括 T68⑧-b 想验证的"文件被删后 F5 降级提示"这个场景本身，在文档仍打开的情况下根本无法触发（因为文件删不掉）。

这直接削弱了 F5 功能的实际意义：F5 存在的前提是"外部编辑器改了文件，用户手动刷新看新内容"，但如果外部编辑器保存不了，这个场景不会发生。

**这是架构取舍问题（零拷贝 vs 允许外部写入），不是我能自行决定修的范围，需要你来定**——可能的方向（仅供参考，我没有做任何代码改动）：
- 给 `FileMap::Open` 的 `CreateFileW` 加上 `FILE_SHARE_WRITE | FILE_SHARE_DELETE`，允许外部读写/删除，但这样"文件被外部替换后 mdvn 仍持有旧映射"要额外处理（旧映射内容不会自动更新，F5 时需要重新 Open 一次全新映射，这个逻辑 T70 应该已经做了，因为它是"重新走一遍打开流程"）
- 或者接受现状，把 F5 的实际用途改为"文件被外部工具重命名后再放回原路径"这种边缘场景，但这样功能价值大幅缩水
- 我没有测试"mdvn 只读打开、不持有映射到文档关闭"的替代设计是否可行——如果零拷贝设计要求映射必须常驻，这可能是个必须接受的固有限制

**F5 在"关闭旧文档→打开新文档"这个替换场景下本身没问题**：单测（`ClampReloadTopBlockIndex`、`ReloadCurrentDocument` 走 `openDocumentInPlace`）已经验证过重载流程本身能跑通、不崩溃、能恢复滚动位置——问题是外部修改这个文件的动作本身被 Windows 拒绝了，压根走不到 F5 那一步。

### ⑧-c 连按 100 次的内存曲线

未测——⑧-a/b 卡住后判断继续测这一项意义不大（既然内容更新场景本身触发不了，连按 F5 只是重复加载同一份未变化的文件，内存曲线测的是"reload 同内容"而不是"reload 后真的换了内容"，参考价值有限）。如果⑧-a/b 的架构问题有了处理方案，建议连同这一项一起补测。

---

## 总体验收状态

| 项目 | 状态 | 备注 |
|------|------|------|
| ① 截图对照 | ✅ 已测（100% DPI） | 大部分内容类型覆盖，缺查找高亮/大纲侧栏截图 |
| ② 主题切换 | ✅ 已测通过 | 实时跟随、无闪烁 |
| ③ 首帧不白闪 | ✅ 已测通过 | 高频截图代替专业录屏，观察到无害的DWM合成残影 |
| ④ 多屏DPI恢复 | ✅ 已测通过（仅100%DPI） | 精确恢复+精确24px层叠偏移；未覆盖150%/200%DPI |
| ⑤ 副屏拔掉 | 待办（刻意跳过） | 风险大于收益，单测已等价覆盖 |
| ⑥ 文件关联验证 | 部分完成 | Win10自动化已过(T61)，Win11无环境，ProcMon未装 |
| ⑦ 高对比度声明 | ✅ 已完成 | 声明+实测截图，行为与声明一致 |
| ⑧ F5重载 | ⚠️ 发现架构级问题 | 文件映射独占导致外部编辑器基本无法改动被打开的文件，需用户决策 |

---

## 截图清单

全部位于 `bench/screenshots/`（已在 `.gitignore` 排除，不进版本控制）：

- `01_initial_light.png` — 启动初始浅色
- `02_light_explicit.png` / `03_dark_explicit.png` — 显式浅/深色
- `04_dark_scrolled.png` / `05_dark_scrolled2.png` / `06_dark_checkbox2.png` / `07_dark_codeblock2.png` — 深色下各内容类型
- `08_system_before_toggle.png` / `09_system_after_toggle.png` — 系统主题实时切换前后
- `10_flash_frame_1.png` ~ `_14.png` — 深色首帧高频截图序列
- `11_secondary_monitor.png` / `12_restored_secondary.png` — 副屏移动与恢复
- `13_f5_before.png` — F5测试前内容
- `14_high_contrast.png` — 高对比度模式下的实际观感

## 备注

仍未覆盖的组合：150%/200% DPI、Win11、副屏物理拔除、ProcMon 注册表监控、查找高亮/大纲侧栏截图、F5 的完整内容更新场景（因⑧的架构问题受阻）。这些留给你决定是否需要、何时补测。
