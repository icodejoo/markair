# mdvn 落地计划

> 阶段划分以"可验证的性能结论"为主线,而不是以功能数量为主线。任何一个阶段如果性能验收不过,先解决性能再往下走。

## M-1:可行性 Spike(1~2 天,唯一允许写代码的立项阶段动作)

**目的**:验证 15MB / 60ms 目标是否现实。不写产品代码,产出放在 `spikes/` 目录,和正式代码物理隔离,不进主构建。

| Spike | 内容 | 要回答的问题 |
|---|---|---|
| `spikes/s01_d2d_baseline` | 一个 Win32 窗口 + D2D 上下文 + DirectWrite 画一行 "Hello 中文" | 空壳程序的 Private Working Set 基线是多少?进程入口到首个 Present 多少 ms? |
| `spikes/s02_font_probe` | 对比"只 CreateTextFormat 指定族名" vs "调用 GetSystemFontCollection 枚举" | DirectWrite 是否会把字体数据带进私有工作集?枚举的代价是多少? |
| `spikes/s03_md4c` | 把 md4c vendored 进来,解析 100KB 与 10MB 文档,回调里只计数 | exe 体积增量?解析耗时?峰值内存? |

**Go / No-Go 判据**:s01 基线 ≤ 8MB 且首帧 ≤ 40ms,s03 解析 100KB ≤ 5ms。若基线就超 15MB,需回到需求文档调整性能目标(而不是硬撑)。

---

## M0:最小可渲染(目标 1~2 周)

**范围**:打开一个 md,渲染标题/段落/粗斜体/行内代码/列表/引用/分割线/围栏代码块(无高亮),纯文本布局 + 滚动。

**验收标准**
- BENCH-A 暖启动首屏 ≤ 80ms(M0 放宽,M3 收紧到 60ms)
- Private Working Set ≤ 20MB(M3 收紧到 15MB)
- 滚动无卡顿(肉眼 + PresentMon 抽查)
- 单元测试:编码嗅探、arena、文档模型构建三块有测试且通过

**风险与验证**
| 风险 | 验证 |
|---|---|
| 中英混排换行/断行不正确 | 准备一份中英混排 + 长 URL + 代码的测试文档,人工视觉比对 |
| 滚动时重排导致掉帧 | PresentMon 抓 1000 帧,看 99 分位 |
| 高 DPI / 多显示器不同 DPI 拖动 | 手动在 100%/150%/200% 与双屏拖动测试 |

---

## M1:基础 GFM(目标 2~3 周)

**范围**:表格、任务列表、删除线、自动链接、脚注、图片(本地 + data URI)、链接点击行为(外链/相对 md 跳转/锚点)、`Ctrl+F` 查找、字号缩放、YAML front matter 隐藏。

**验收标准**
- 用一组真实世界文档回归:至少 20 个来自知名开源项目的 README/CHANGELOG,渲染结果与 GitHub 网页版逐项人工比对,差异记录成 issue 并分类(可接受/必修)
- 图片密集文档(50 张 PNG)内存 ≤ 80MB,滚走后回落到 ≤ 30MB
- CommonMark 一致性:md4c 层由上游保证;我们只测"回调 → 模型 → 布局"不丢信息(写快照测试)

**风险与验证**
| 风险 | 验证 |
|---|---|
| 表格列宽算法(内容自适应 vs 等分)难做好看 | 先用"内容测宽 + 上限约束",拿真实 README 里的宽表格试 |
| 图片缓存策略导致滚动跳动 | 保留尺寸占位,滚动位置不因图片释放而变 |
| 恶意文档(超深嵌套/百万链接) | 写 fuzz 语料跑,配合 ASAN(clang-cl)与 Application Verifier |

---

## M2:文件关联 / 主题 / 体验(目标 2 周)

**范围**:`.md` 文件关联注册与卸载、窗口状态记忆、主题(见待确认项)、代码块极简语法高亮、大纲侧栏(若确认要)、历史前进后退、拖放打开。

**验收标准**
- 从"干净虚拟机安装 → 双击 md 打开"全流程走通,卸载后注册表无残留(用 Process Monitor 对比前后)
- 高亮开启后 exe 体积增量 ≤ 80KB,BENCH-A 首屏时间增量 ≤ 3ms
- 主题切换无需重启

**风险与验证**
| 风险 | 验证 |
|---|---|
| 文件关联在 Win10/Win11 上的行为差异(默认程序策略收紧) | 两个系统各自实测;必要时改为"引导用户到系统设置"而非静默写注册表 |
| 高亮误标记影响阅读 | 用 10 种语言的样例文件人工过一遍 |

---

## M3:性能打磨与发布(目标 1~2 周)

**范围**:达成需求文档 §4 的全部硬性指标;超大文档优化;软件渲染回退路径;打包。

**验收标准**:需求文档 §4 表格全部通过,写成一份可复跑的性能报告。

**测量方法(固定下来,每个版本跑一次)**

| 指标 | 工具 | 方法 |
|---|---|---|
| 启动时间 | 内置埋点(`QueryPerformanceCounter`,进程入口 → 第一次 `Present` 返回)+ 环境变量开启后输出到 stderr | 跑 20 次取中位数与 P95;暖/冷各一组,冷启动用 **RAMMap** 的 Empty Standby List + 重启进程 |
| 启动时间(外部佐证) | **Windows Performance Recorder / WPA**(ETW),或 `xperf` | 看 DLL 加载、磁盘 IO、首帧时间线,定位瓶颈 |
| 内存 | **VMMap**(Sysinternals) | 记录 Private Working Set / Private Bytes / Mapped File 三项,区分"真私有"与"共享映射" |
| 内存(自动化) | `GetProcessMemoryInfo` 的 `PrivateUsage`,内置 `--bench` 开关输出 | 进 CI,做回归阈值门禁 |
| 帧率 | **PresentMon** | 滚动脚本化(模拟 WM_MOUSEWHEEL),取 99 分位帧时间 |
| 内存泄漏 | Application Verifier + CRT 调试堆 / clang-cl ASAN | 反复打开关闭 100 个文档,看内存是否单调上升 |
| exe 体积 | 构建产物 + `link /dump /headers` | CI 门禁 |

**风险**
| 风险 | 缓解 |
|---|---|
| 软件渲染(RDP/无 GPU)达不到 60FPS | 降级为"滚动时降低重绘精度";实在不行才评估 GDI 路径 |
| 杀软对新 exe 的扫描拖慢冷启动 | 这是不可控外因,测量时单独标注;不因此引入加壳 |
| 目标达不成 | 提前(M0 结束时)就应该知道;届时与用户重新协商指标而不是无限延期 |

---

## 阶段依赖与并行

```
M-1 Spike ──→ M0 ──→ M1 ──→ M2 ──→ M3
              │        │
              └─ 性能测量脚手架(内置 --bench + CI 门禁)在 M0 就建好,之后每阶段都跑
```

---

## 需要用户确认的决策点(阻塞对应阶段)

| # | 决策点 | 阻塞阶段 | 我的技术建议(仅供参考,不替你定) |
|---|---|---|---|
| 1 | 是否支持 mermaid | M1 范围 | 不支持。前期实测已证明这是内存爆炸的直接诱因 |
| 2 | 是否支持 LaTeX 数学 | M1 范围 | 不支持 |
| 3 | 代码高亮是否要,覆盖哪些语言 | M2 | 要,但用自研极简方案,覆盖 8~10 种 |
| 4 | 深色模式:跟随系统 / 手动 / 不做 | M2 | 纯偏好。技术上跟随系统成本很低(`ShouldAppsUseLightTheme` + `WM_SETTINGCHANGE`) |
| 5 | 网络图片默认加载与否 | M1 | 默认不加载(隐私 + 启动时延),提供开关 |
| 6 | 单实例复用窗口 vs 每文件新窗口 | M0 | 纯偏好 |
| 7 | 是否要大纲侧栏 / 同目录快速切换 | M2 | 技术成本低,体验收益明显,但属功能范围决策 |
| 8 | 打包形态:绿色 exe / MSI / 都要 | M3 | 绿色单 exe 最符合定位;文件关联可用一个 `--register` 命令行开关 |
| 9 | UI 外观(标准标题栏 vs 自绘极简) | M0 | 纯偏好。自绘要多写不少非核心代码 |
| 10 | 正文/中文/等宽的默认字体族 | M0 | 见架构文档 §4 的白名单建议 |
| 11 | 是否接受引入测试框架(gtest/catch2,只影响测试产物) | M0 | 倾向自写极简断言宏以保持零依赖,但引入也无害 |
| 12 | 性能指标若 M0 实测达不到,是放宽指标还是加投入 | M0 结束 | 建议放宽到实测可达的水平,把目标定成"可验证的承诺" |
| 13 | 许可与是否开源 | 随时 | 若开源需在发布物中附 md4c 的 MIT 许可声明 |
