# mdvn 需求文档 / 立项脑暴

> 状态:**范围已定稿**(立项阶段)。原先标注 **[待确认]** 的 13 项决策已于 2026-09-16 裁决完毕,见 §8。
>
> **裁决原则(2026-09-16 用户授权)**:
> > "mdvn 会增加显著内存的放弃,如果增加一点点换来使用体验就保留。"
>
> 本文档中标注 **[已裁决 2026-09-16]** 的条目均依据该授权原则做出,用户未逐条复核。若某项决策不属于"内存 vs 体验"维度(如分发形态、许可),会在 §8 中显式注明"该原则不适用"并给出所依据的其他技术理由。

## 1. 一句话定位

Windows 专用、只读的 Markdown 查看器。双击 `.md` 文件 **瞬间** 打开,常驻内存比现有工具低一个数量级。

## 2. 目标用户画像

| 画像 | 场景 | 痛点 |
|---|---|---|
| 开发者 | 在资源管理器里翻 README / CHANGELOG / 设计文档 | VS Code 开一个 md 要几秒 + 几百 MB;Typora 之类偏编辑器 |
| 技术写作者/PM | 收到别人发的 md 附件,只想看一眼 | 不想为"看一眼"装一个重型编辑器 |
| 老机器 / 低配轻薄本用户 | 8GB 内存,同时开着 IDE + 浏览器 | 任何额外 100MB+ 都心疼 |

共同点:**只看不改**。需要改的时候他们会用自己惯用的编辑器。

## 3. 核心场景(MVP 必须覆盖)

1. 在资源管理器双击 `.md` → 窗口出现并完成首屏渲染。
2. 命令行 `mdvn README.md` 打开。
3. 拖拽一个 `.md` 到已打开的窗口 → 替换当前文档。
4. 阅读态操作:滚轮/PageUp/PageDown/Home/End 滚动、`Ctrl+F` 查找(见下)、`Ctrl+ +/-` 缩放字号、`Ctrl+W`/`Esc` 关闭。
5. 外部编辑器保存了同一个文件 → 自动热重载并尽量保持滚动位置。

## 4. 性能目标(硬性验收标准)

测试基准文件定义为 **BENCH-A**:约 100 KB 的纯文本 Markdown(典型大型 README 量级,含表格、代码块、若干行内链接,无图片)。
测试机基线:SATA/NVMe SSD + 4 核以上 CPU + Windows 10/11 x64,测量取**第二次及以后**打开(排除首次冷启动的磁盘缓存缺失),并单独记录真·冷启动数据。

| 指标 | 目标值 | 上限(超过即不通过) | 测量方式 |
|---|---|---|---|
| 首屏可见时间(暖启动,BENCH-A) | ≤ 60 ms | 120 ms | ETW / `QueryPerformanceCounter` 埋点:进程入口 → 第一次 `Present` 返回 |
| 首屏可见时间(冷启动,清空 standby 缓存) | ≤ 250 ms | 400 ms | 同上 + `RAMMap` 清缓存 |
| 常驻内存 Private Working Set(BENCH-A,打开后静置 10s) | ≤ 15 MB | 25 MB | VMMap / `GetProcessMemoryInfo` 的 `PrivateUsage` |
| 常驻内存(空文档/刚启动) | ≤ 8 MB | 12 MB | 同上 |
| 可执行文件体积(单文件,静态链接 CRT) | ≤ 1.5 MB | 3 MB | 文件属性 |
| 滚动帧率 | 60 FPS 无掉帧(BENCH-A) | 平均 < 55 FPS 即不通过 | PresentMon |
| 10 MB 超大文档打开时间 | ≤ 1.5 s 且 UI 不卡死 | 3 s | 同首屏埋点 |
| 子进程数 | 恒为 0 | 出现任何子进程即不通过 | 任务管理器 / Process Explorer |

对比参照(前期实测,写入文档作为基线):Ghostwriter ~150 MB、mdr(WebView2 后端)~66 MB 主进程 + 多个 WebView2 子进程、mdr(gui/egui 后端)~440 MB、mdviewer(egui)~95 MB、MDReader ~93 MB。**mdvn 的目标是 15 MB 级,即比目前最好的第三方方案再低 6 倍以上。**

> 注:15 MB 目标的可行性依赖"不加载浏览器内核、不全量扫描系统字体、不常驻位图缓存整篇文档"这三条,见架构文档。若 M0 spike 实测无法达成,应先调整目标再继续,而不是无限期堆优化。

## 5. MVP 功能范围

### 5.1 必须有(In Scope)

- CommonMark 0.31 基础块级/行内元素:标题、段落、列表(有序/无序/嵌套)、引用块、代码块(围栏 + 缩进)、水平线、强调/加粗、行内代码、链接、图片、硬换行。
- GFM 扩展(详见 §6)。
- UTF-8 / UTF-8 BOM / UTF-16LE BOM 自动识别;无 BOM 且非法 UTF-8 时回退到系统 ANSI 代码页(避免中文文档乱码)。
- 文件热重载(监听所在目录的写入事件,带 100~200 ms 去抖)。
- 窗口状态记忆:位置、大小、字号缩放级别。存 `%LOCALAPPDATA%\mdvn\state.ini`(纯文本、无注册表写入)。
- 文件关联:可选安装/卸载 `.md` / `.markdown` 关联(不静默抢占,由用户在设置或命令行显式执行)。
- 查找:`Ctrl+F` 在渲染后的纯文本中做子串查找 + 高亮 + 上下跳转。
- 链接点击行为:`http(s)` 用默认浏览器打开;相对路径 `.md` 链接在本窗口内跳转;`#锚点` 滚动到对应标题。

### 5.2 明确非目标(Non-Goals,MVP 不做)

| 非目标 | 理由 |
|---|---|
| 任何编辑能力 | 定位就是查看器;编辑意味着撤销栈、输入法、光标、保存冲突,复杂度翻倍 |
| 多标签页 / 多文档界面 | 一个文件一个窗口,符合"双击即开"的心智;以后要做也是 M3+ |
| 实时协作 / 云同步 | 与定位无关 |
| 导出 PDF / HTML | 导出 PDF 基本等同于内嵌一个排版/浏览器引擎(MDReader 就是因此变重),直接排除 |
| 插件系统 / 脚本扩展 | 会把启动期变成动态加载期,与性能目标冲突 |
| 自动更新程序 | 常驻后台/额外进程,与"单进程、零后台"冲突 |
| 跨平台 | 用户明确只要 Windows |
| 目录树 / 工作区侧栏 | **[已裁决 2026-09-16]不做**。目录树要 `FindFirstFileW` 枚举 + 常驻一份文件项列表 + 一套列表 UI(选中态/滚动/图标),属于"为了顺带功能新增一个常驻数据结构和一套交互",不是"一点点"。大纲侧栏另行保留(见 §5.3) |
| Mermaid 图表渲染 | **[已裁决 2026-09-16]不做**,退化为普通代码块,见 §6 |
| LaTeX 数学公式排版 | **[已裁决 2026-09-16]不做**,退化为等宽原文,见 §6 |
| SVG 图片渲染 | **[已裁决 2026-09-16]不做**,显示占位框,见 §6 图片策略 |

### 5.3 原"建议讨论项"的裁决结果 **[已裁决 2026-09-16]**

1. **文档大纲(TOC)侧栏 —— 纳入 M2,默认关闭(`Ctrl+\` 切换)**。
   内存代价可量化:标题的层级与文本切片在解析阶段已经进了文档模型(`Block{type=H1..H6}`),侧栏不新增任何文档数据,只多一个覆盖侧栏区域的 `IDWriteTextLayout`(典型 README 30 条标题,约 20~40 KB)+ 一个 `int32` 索引数组。默认关闭时这些对象根本不创建,开销严格为 0。属于"一点点换体验"。
2. **相对链接跳转的历史栈(前进/后退,`Alt+←/→`)—— 纳入 M2**。
   §5.1 已承诺"相对路径 `.md` 链接在本窗口内跳转",一旦能跳转就必须能回退,否则是功能缺陷而非增项。历史栈只存路径字符串 + 滚动偏移,上限 32 条,内存约 8 KB。
3. **"列出同目录全部 md 并快速切换" —— 不做**。见 §5.2 目录树条目,理由相同。
4. **单实例模式 —— 不做,每个文件一个独立窗口(独立进程)**。
   这条需要说明它为什么不违背原则:单实例能在"同时看两个文档"时省下一个进程的 ~15 MB,但代价是打开第二个文档会**顶掉**第一个文档的内容——而目标用户(开发者对照 README 与 CHANGELOG)恰恰需要并排。为省 15 MB 牺牲核心场景不划算。同时需澄清:§4 的指标口径已相应改为"**子进程数恒为 0**" —— 约束的是**单个文档实例不得派生子进程**(对比 WebView2 的多进程模型),而不是"全机只允许一个 mdvn 进程"。
   实现上仍需最低限度协调:用命名互斥体检测是否已有实例,仅用于"同一个文件被重复双击时前置已有窗口"(`SetForegroundWindow`),不转发、不复用窗口。代价为一个内核对象句柄。

## 6. GFM 支持范围 **[已裁决 2026-09-16,全节定稿]**

| 特性 | 结论 | 内存/复杂度判断依据 |
|---|---|---|
| 表格 | **M1 支持** | 只新增列宽度量数组(列数 × 4 字节);缺了 README 不可用 |
| 任务列表 `- [x]` | **M1 支持**(渲染成勾选框图形,不可点击) | md4c 原生支持,勾选框用 D2D 画两条线,零字体、零位图 |
| 删除线 `~~x~~` | **M1 支持** | 一个 `Inline.flags` 位 + `IDWriteTextLayout::SetStrikethrough`,零增量 |
| 自动链接(裸 URL) | **M1 支持** | md4c permissive autolink flag,零增量 |
| 脚注 `[^1]` | **M1 支持** | md4c 0.6.0 原生支持;只多一个脚注区块 |
| 代码块语法高亮 | **M0/M1 不做(等宽 + 背景块);M2 自研极简词法高亮** | 内存增量可量化:关键字表约 10 语言 × 50 词 × 8 B ≈ 4 KB,位于 `.rdata` 只读段、按需分页,几乎不计入私有工作集;token 数组走 arena 且只对**可见**代码块生成。典型"一点点换体验",**保留** |
| YAML front matter | **默认隐藏**(M1) | 纯解析层跳过,负成本(少渲染一个块) |
| 数学公式 LaTeX | **不做**。`$...$` / `$$...$$` 原样按等宽文本显示 | **[见 §8 #2 的量化论证]** |
| Mermaid 图表 | **不做**。```` ```mermaid ```` 退化为普通代码块(M2 起可享受高亮) | **[见 §8 #1 的量化论证]** |
| HTML 内联块 | **按原样显示为等宽纯文本**,不解释执行 | 解释 HTML 需要 HTML 引擎,数量级等同于内嵌浏览器;也是安全面 |
| Emoji `:smile:` 短码 | **不做**(Unicode emoji 字符本身由系统 Segoe UI Emoji 正常显示) | 需内置约 1800 条短码→码点映射表(约 40 KB 常驻),而收益只是少数文档里的少量图标;属于"增量不小、体验增益低",按原则放弃 |

### 图片加载策略 **[已裁决 2026-09-16]**

| 类型 | 结论 | 判断依据 |
|---|---|---|
| 本地相对/绝对路径图片 | **M1 加载**,WIC 解码,单图上限 4096×4096,超限显示占位框 | 图片是 README 的刚需(徽章、截图);WIC 是系统组件且延迟加载,无图文档零开销 |
| 网络图片 `http(s)://` | **默认不加载**,显示占位块 + "点击加载";`state.ini` 提供 `load_remote_images=0/1` 开关 | 主因不是内存而是**隐私与启动时延**:打开一个 md 就向第三方暴露 IP;网络 RTT 不可控,会直接击穿 60ms 首屏目标。WinHTTP 走 `/DELAYLOAD`,不点就不加载该 DLL |
| data: URI 图片 | **M1 支持** | 无网络行为,解码路径与本地图片完全共用,零新增代码路径 |
| SVG 图片 | **不做**,显示占位框 + alt 文本 | 与 mermaid 同一条矢量渲染链路(见 §8 #1),内存代价同级 |
| 动图 GIF | **只渲染首帧** | 动画需要常驻定时器 + 持续重绘 + 全帧位图常驻(一张 500×300 的 30 帧 GIF 解码后约 18 MB,单张就超过整个内存目标),按原则放弃 |
| 图片缓存 | LRU,上限默认 64 MB,可在 `state.ini` 配置 | 滚出视口很远的位图释放,保留尺寸信息以免布局跳动 |

## 7. 质量与安全要求

- 打开恶意/畸形 md(超深嵌套、超长行、非法编码、几十万个链接)不得崩溃或卡死;应有嵌套深度与节点数上限并优雅降级。
- 不做任何主动网络请求(除非用户显式点击加载网络图片/打开外链)。
- 不写注册表,除非用户显式执行文件关联注册。
- 无遥测、无后台常驻进程、无自动更新。

## 8. 决策裁决记录 **[2026-09-16 定稿]**

> 以下 13 项原为"待用户确认"。用户于 2026-09-16 授权按下述原则代为裁决,未逐条复核:
> **"mdvn 会增加显著内存的放弃,如果增加一点点换来使用体验就保留。"**
>
> 每项均给出可量化的判断依据。**#8、#9、#13 三项不属于"内存 vs 体验"维度**,已在该项中显式说明原则不适用,并给出所依据的其他技术理由。

### #1 Mermaid 图表 —— ❌ 不做,退化为代码块

原则适用:**显著内存增加**。量化依据:

- Mermaid 本体是 JavaScript 库,**不存在可用的原生 C++ 实现**(2026-09 检索确认)。唯一的嵌入路径是 "JS 引擎 + SVG 渲染器 + 矢量字体形状链路":现有的非浏览器方案(如 `mermaidx`)正是 **QuickJS + resvg** 这一组合。
- `resvg`/`usvg` 需要 `fontdb` 做字体解析与字形轮廓提取,**这正是前期实测中 mdr 的 gui 后端达到 440 MB 的直接成因**(见 §4 基线表),相对 15 MB 目标是 **约 29 倍**。
- 即便自研一个只覆盖 flowchart 的极简布局器,也必须引入"字形轮廓 + 路径填充 + 自动布局求解"三块全新子系统,与"只读文本查看器"的定位不成比例。

处理方式:```` ```mermaid ```` 按普通围栏代码块渲染(M2 起可享受语法高亮),阅读者仍能看到原始定义。

### #2 LaTeX 数学公式 —— ❌ 不做,原样显示

原则适用:**显著内存 + 显著体积增加**。量化依据:

- 最成熟的可嵌入 C++ 方案 **MicroTeX / cLaTeXMath** 的依赖链为 Cairo + Pango + FontConfig + HarfBuzz + FreeType + pixman + libpng(2026-09 检索确认),单是这条链就远超"零外部依赖"的构建约束。
- 更致命的是**字体**:数学排版必须内嵌 XITS / FiraMath / TeX Gyre / Latin Modern 之一,单套 TTF 数百 KB 至 1 MB+,**仅内嵌字体一项就击穿 §4 的 1.5 MB exe 上限**;且 Windows 不自带任何数学字体,无法像正文那样靠系统共享映射规避私有内存。
- FontConfig 在 Windows 上会扫描字体目录建缓存 —— 正是架构文档 §4 明令禁止的 fontdb 反模式。

处理方式:`$...$` / `$$...$$` 按等宽文本原样显示(md4c 的 LaTeX math span 扩展**不启用**,避免解析器把它吃掉)。

### #3 代码块语法高亮 —— ✅ 要,M2 自研极简词法高亮

原则适用:**一点点换体验**。量化依据:

- 方案为自研 5 类 token(关键字/字符串/数字/注释/其他)的词法器,**无任何第三方依赖**(已排除 Lexilla 体积、tree-sitter 每语言数百 KB 的 grammar,见技术选型 §4)。
- 静态数据量:约 10 种语言 × 50 关键字 × 8 B ≈ **4 KB**,编译进 `.rdata` 只读段,按需分页,几乎不计入 Private Working Set。
- 动态数据量:token 数组走 arena,且只为**视口内**的代码块生成,随视口而非文档长度增长。
- 体验增益明确:开发者是首要用户画像,代码块是 README 的主体内容之一。

覆盖语言(首批 11 种):C、C++、C#、Java、JavaScript/TypeScript、Python、Go、Rust、JSON、YAML、Shell。未识别的语言标记按无高亮渲染。
M2 验收沿用交付计划:exe 体积增量 ≤ 80 KB,BENCH-A 首屏时间增量 ≤ 3 ms。

### #4 深色模式 —— ✅ 要,跟随系统 + 手动三态覆盖(M2)

原则适用:**几乎零内存换明确体验**。量化依据:

- 跟随系统仅需读注册表 `HKCU\...\Themes\Personalize\AppsUseLightTheme` 一个 DWORD,并响应 `WM_SETTINGCHANGE`。
- 常驻数据是一个颜色表结构体(约 15 个 `D2D1_COLOR_F`,**不到 300 字节**),主题切换只改指针不重排版。
- 标题栏跟随用 `DwmSetWindowAttribute(DWMWA_USE_IMMERSIVE_DARK_MODE)`,系统 API,零成本。

行为:`state.ini` 中 `theme = system | light | dark`,默认 `system`;`Ctrl+Shift+T` 循环切换,切换无需重启。

### #5 网络图片默认加载 —— ❌ 默认不加载,提供开关(M1)

此项主因不是内存,而是**隐私与启动时延**(已在 §6 图片策略中说明)。要点:

- 打开一个别人发来的 md 就向第三方服务器暴露 IP,是隐私问题,与 §7 "不做任何主动网络请求"的承诺直接冲突。
- 网络 RTT 完全不可控,同步等待会直接击穿 60 ms 首屏目标;异步加载则导致首屏后布局跳动。
- 内存维度上倒是"一点点":WinHTTP 走 `/DELAYLOAD`,用户不点就不加载该 DLL,零常驻开销 —— 所以**保留手动加载入口**(点击占位块加载)与 `state.ini` 的 `load_remote_images` 开关,而不是彻底禁掉。

### #6 单实例 vs 每文件新窗口 —— ❌ 不做单实例,每文件独立窗口

原则适用,但结论指向"多花内存":单实例可在双开时省约 15 MB,但会**顶掉**第一个文档的内容,直接破坏"对照阅读 README 与 CHANGELOG"这一核心场景。为省 15 MB 牺牲核心场景不划算。详见 §5.3 第 4 条(含"进程数恒为 1"的口径澄清)。

保留的最低协调:命名互斥体仅用于"同一文件被重复双击时前置已有窗口",代价为一个内核对象句柄。

### #7 大纲侧栏 / 同目录切换 —— ✅ 大纲侧栏做(M2,默认关);❌ 同目录文件列表不做

详见 §5.3 第 1、3 条。要点:大纲复用已有的标题模型数据,默认关闭时开销严格为 0,开启后约 20~40 KB;同目录列表需要目录枚举 + 常驻文件项列表 + 一套列表 UI,不属于"一点点"。
相对链接跳转的**历史栈(前进/后退)** 一并纳入 M2(§5.3 第 2 条),它是已承诺功能的必要组成,不算增项。

### #8 打包形态 —— 绿色单 exe(+ `--register` 开关)

> ⚠️ **"内存 vs 体验"原则在此不适用** —— 打包形态是分发方式问题,MSI 与绿色 exe 的**运行时内存完全相同**。以下结论基于其他技术理由:

- **绿色单 exe**。静态链接 CRT 后无运行时依赖,可直接放进 U 盘/共享目录,零安装。
- 文件关联通过 `mdvn --register` / `mdvn --unregister` 显式执行,与 §7 "不写注册表,除非用户显式执行"的承诺一致;MSI 的安装器天然倾向于静默写注册表,反而与该承诺冲突。
- 不做 MSI:会引入 WiX 工具链、升级码/组件 GUID 管理与卸载逻辑的长期维护成本,对一个单文件工具收益极低。
- 代码签名视后续是否公开发布再定,不阻塞 M3。

### #9 UI 外观 —— 标准 Windows 标题栏

> ⚠️ **"内存 vs 体验"原则在此基本不适用** —— 自绘标题栏与标准标题栏的内存差异在 100 KB 以内,不构成取舍依据。以下结论基于开发成本与系统集成度:

- **用标准 Windows 标题栏**。自绘需要自行处理 `WM_NCCALCSIZE`/`WM_NCHITTEST`、最大化边距、Aero Snap 与 Win11 Snap Layouts、高对比度主题、触摸拖拽、多 DPI 下的按钮命中区 —— 全是与核心价值无关的非核心代码,且极易在某个 Windows 版本上出现回归。
- 标准标题栏配合 `DWMWA_USE_IMMERSIVE_DARK_MODE`(见 #4)在深色模式下观感已经足够好。
- 内容区保持极简:无菜单栏、无工具栏、无状态栏(查找框与大纲侧栏按需浮出)。

### #10 默认字体族 —— 采纳架构文档 §4 白名单,允许 ini 覆盖

原则适用:白名单方案本身就是为规避字体全量扫描而设计的,零额外内存。定稿如下(与架构文档 §4 一致,不作修改):

- 正文西文:`Segoe UI`
- 正文中文:`Microsoft YaHei UI` → `Microsoft YaHei` → `SimSun`
- 等宽:`Cascadia Mono` → `Consolas` → `Courier New`

补充:`state.ini` 允许用户覆盖这三条链的族名(纯字符串,零内存代价),但**不提供字体选择下拉框** —— 下拉框意味着 `GetSystemFontCollection` 枚举,是架构 §4 明令禁止的反模式。
⚠️ 该白名单成立的前提是"`CreateTextFormat` + 自定义 `IDWriteFontFallbackBuilder` 不触发全量字体枚举",此假设由 M-1 的 `spikes/s02_font_probe` 实测验证,见 `05-m0-tasks.md`。

**2026-09-17 修订 —— 等宽链追加中文回退**

原裁决把等宽链定为纯西文(`Cascadia Mono` → `Consolas` → `Courier New`),现修订为在其后**追加**中文回退,最终优先级:

- 等宽:`Cascadia Mono` → `Consolas` → `Courier New` → `Microsoft YaHei UI` → `Microsoft YaHei` → `SimSun`

理由:

- 代码块里出现中文是真实使用场景(中文注释、中文字符串字面量、mermaid/流程图的中文节点名等)。纯西文链下这些字符一个字形都匹配不到,DirectWrite 全部画成方块(tofu),`bench/demo.md` 的 mermaid 示例已实测复现。
- 追加的三个族名就是正文中文链已经在用的那三个,**复用已加载的字体资源,不新增任何字体文件**,代价接近零 —— 这与"emoji 短码需要新增约 40 KB 映射表"那类"为边缘收益付出实打实内存"的情况性质不同,不受"内存 vs 体验"原则的否决。
- 西文等宽族保持在中文族之前,西文代码仍由等宽字体承接,不会被中文字体的非等宽字形接走,代码块的等宽对齐观感不变。
- 白名单性质未变(仍是固定 6 个族名,不做 `GetSystemFontCollection` 枚举),上面那条"不触发全量枚举"的前提继续成立。

### #11 测试框架 —— ❌ 不引入,自写极简断言宏

原则适用于**构建复杂度**而非运行时内存(测试产物不影响主 exe):

- 引入 gtest/catch2 需要 vcpkg 或 git submodule,**直接破坏技术选型 §5 "构建零外部依赖、md4c 直接 vendored"这条已定结论**,代价是整个构建链路的性质改变,不是"一点点"。
- 自写断言宏的实际需求很小:`MDVN_CHECK(cond)` / `MDVN_CHECK_EQ(a,b)` / 一个测试注册表宏 + 一个 `main`,约 80 行,产出单个 `mdvn_tests.exe`。
- 若后续测试规模膨胀到自写方案吃力,再单独评估 —— 那时的迁移成本也不高(断言宏可直接映射到 catch2)。

### #12 M0 内存目标是否预先放宽 —— ❌ 不放宽,维持现有门槛

原则适用,且原则本身要求目标线可信:如果先把线放松,"显著增加内存就放弃"就失去了判断基准。

- M0 门槛维持 **Private Working Set ≤ 20 MB、BENCH-A 暖启动首屏 ≤ 80 ms**;M3 收紧到 15 MB / 60 ms。
- 调整目标的唯一触发条件是**实测数据**,而非预判:按交付计划 M-1 的 Go/No-Go 判据,若 `s01_d2d_baseline` 空壳基线就超过 8 MB,则**立即回到本文档 §4 与用户重新协商指标**,而不是硬撑或事后放宽。
- 也就是说:这项不是"现在决定放不放宽",而是"把放宽的决策权留到有数据的那一刻"。

### #13 许可与开源 —— MIT 开源

> ⚠️ **"内存 vs 体验"原则在此不适用** —— 这是法务与分发维度。以下结论基于依赖合规与项目性质:

- 采用 **MIT**,与 vendored 的 md4c 许可一致,避免许可兼容性讨论。
- 发布物(exe 同目录或"关于"信息)中必须附 md4c 的 MIT 许可声明与所采用的版本号,这是 md4c 许可的硬性要求。
- `third_party/md4c/` 下保留上游 `LICENSE` 与 `VERSION.txt`。
- 若用户后续希望闭源,MIT 依赖不构成障碍(仍需保留 md4c 声明),该决策可随时反悔,不阻塞任何阶段。
